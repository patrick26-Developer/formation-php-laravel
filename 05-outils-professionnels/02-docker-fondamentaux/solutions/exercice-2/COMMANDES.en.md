# Commands — Exercise 2

```bash
# From a folder containing this Dockerfile AND a copy of the Calculator mini-project
docker build -t ma-calculatrice .
docker run ma-calculatrice
# Prints: 10 + 5 = 15
```
